generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlserver"
  url      = "sqlserver://nvhealth.database.windows.net:1433;database=nvdb;user={nvhealth.database.windows.net/nvadmin};password=Nvhealth23;encrypt=true"
}

model user {
  userid   Int     @id @default(autoincrement())
  name     String  @db.Text
  role     String  @db.Text
  email    String  @db.Text
  password String  @db.Text
  mfa      Boolean
}
